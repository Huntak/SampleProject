package com.sample.qna.common.utils;

public class PagingUtils {
	/*public static BoardVO paging(BoardVO qnaVO, int boardCount){
		qnaVO.setTotalCount(boardCount);
		qnaVO.setTotalPage(qnaVO.getTotalCount() / qnaVO.getQuantity());
		if (qnaVO.getTotalCount() % qnaVO.getQuantity() > 0) {
			qnaVO.setTotalPage(qnaVO.getTotalPage() + 1);
		}
		qnaVO.setStart(qnaVO.getPage() * 10 - qnaVO.getQuantity() + 1);
		qnaVO.setEnd(qnaVO.getPage() * 10);
		return qnaVO;
	}*/
}
